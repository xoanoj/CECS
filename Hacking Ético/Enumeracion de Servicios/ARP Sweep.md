Parte de [[Info Gathering]] y [[Enumeracion de Servicios]]

Solo funciona en redes locales, pero no depende de que los hosts objetivo respondan a peticiones ICMP echo request.

``` bash
sudo arping -i [net] -c [packCt] [IP]
```

